@{
    ViewData["Title"] = "Thank You";
    Layout = "_Layout";
}

<div class="container text-center my-5">
    <h1 class="text-success">Thank you for your order!</h1>
    <p class="lead mt-3">Your order has been placed successfully. We’ll notify you once it's processed.</p>
    <a href="@Url.Action("Index", "Home", new { area = "Client" })" class="btn btn-primary mt-4">Continue Shopping</a>
</div>

@section Scripts {
    <script>
        // Clear the cart from local storage
        localStorage.removeItem("cart");
        console.log("Cart cleared");
    </script>
}